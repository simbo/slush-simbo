(function($, window, document, undefined) {
    'use strict';

    var $window = $(window),
        $document = $(document);

    $document.on({
        ready: function(ev) {

        }
    });

})(jQuery, window, document);
